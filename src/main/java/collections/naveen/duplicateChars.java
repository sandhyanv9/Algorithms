package collections.naveen;

import java.util.ArrayList;
import java.util.Arrays;

public class duplicateChars {

   // String str = "abcdabcd";
  //  char[] ch = str.toCharArray();

    ArrayList<Character> charArray = new ArrayList<Character>(Arrays.asList('a','b','c','d','e','f','g'));
  //  charArray.retainAll(Arrays.asList('a', 'e', 'g'));

    //How to find the duplicate Character in a String -- done
    //How to find all the permutations in a string
    //find the lenght of the longest substring without repeating characters?
    //how to remove the duplicate character from a string
    //Write a Comparator in Java to compare two employees based upon their name , id and age

}
